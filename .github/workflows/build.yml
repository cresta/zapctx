name: BuildTest

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  test:
      - name: Check out code
        uses: actions/checkout@v3
      - name: Test code
        uses: cresta/action-lint-go-external@v1